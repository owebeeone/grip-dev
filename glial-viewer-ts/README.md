# glial-viewer-ts

Generic React viewer for routed Glial shared sessions.

The app keeps:

- a raw shared-session Grok that mirrors the routed shared projection
- a viewer Grok with stable UI-focused grips for sessions, contexts, taps, leases, and diagnostics

This app is intentionally separate from `grip-react-demo`.
