# glial-viewer-ts instructions

- Follow the same TS/Vite structure and testing style as `grip-react-demo`.
- Keep the viewer generic: canonical IDs and JSON values first, application-specific assumptions last.
- Preserve the separation between the raw shared-session Grok and the viewer Grok.
