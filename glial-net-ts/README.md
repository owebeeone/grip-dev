# glial-net-ts

Client-side Glial protocol helpers for TypeScript runtimes.

This package currently provides:

- fetch-based HTTP client for the initial Glial router
- `GripSessionLink` implementation for attach and publish flows
- replay and snapshot fetch helpers for early runtime wiring
