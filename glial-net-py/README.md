# glial-net-py

Client-side Glial protocol helpers for Python runtimes.

This package currently provides:

- HTTP client for the initial FastAPI Glial router
- `GripSessionLink` implementation for attach and publish flows
- replay and snapshot fetch helpers for integration tests and early runtime wiring
